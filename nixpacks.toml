[phases.install]
cmds = ["echo 'Skipping package installation; TwinPrimeExplorer.com runs directly from source on Railway.'"]

[start]
cmd = "PYTHONPATH=src python -m dual_prime_explorer --serve --host 0.0.0.0 --port $PORT"
